```
##Pegando o arquivo:
require() -> capta um arquivo externo, mas só funciona com módulos ou arquivos js (similares)
process.argv -> pega os valores do conteudo do terminal e passa para um array
no cmd -> node src/index.js arquivos/texto-web.txt
```
```
fs = file system (biblioteca que lida com arquivos, é nativa do node)

```
